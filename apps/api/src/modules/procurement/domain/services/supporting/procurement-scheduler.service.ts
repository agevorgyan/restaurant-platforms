import { WorkflowContext } from '../../value-objects/workflow/workflow-context.value-object';

export class ProcurementScheduler {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  public scheduleFollowUp(context: WorkflowContext, delayHours: number): void {
    // Emits infrastructure commands to delay or schedule tasks
  }
}